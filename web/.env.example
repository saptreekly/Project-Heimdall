# Local dev (Vite proxies /api when VITE_API_BASE is empty or /api/v1)
VITE_BASE=/
VITE_API_BASE=http://127.0.0.1:8000/api/v1

# GitHub Pages build (set in CI or locally):
# VITE_BASE=/Project-Heimdall/
